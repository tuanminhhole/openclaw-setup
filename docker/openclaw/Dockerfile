# ============================================
# OpenClaw Dockerfile (Template)
# ============================================
# File này sẽ được wizard generate lại với config đúng.
# Nếu bạn muốn setup thủ công, sửa các giá trị bên dưới.
# ============================================

FROM ghcr.io/nicepkg/openclaw:latest

# Plugins sẽ được cài lúc runtime (tránh rate limit từ ClawHub)
# Sửa danh sách plugin và gateway command bên dưới theo nhu cầu

CMD sh -c "openclaw gateway run"
